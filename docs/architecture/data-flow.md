# Data Flow

```{todo}
Document the end-to-end data flow through the system:

- **Sampler**: prompt construction, LLM request/response cycle
- **Evaluator**: code parsing, sandbox execution, parameter optimization
- **Program Database**: registration, Pareto front update, island reset
- **Workers**: multiprocessing queues, message types, distributed coordination
```
